import { Routes } from '@angular/router';
import { ReservationComponent } from './reserve.component';

export const reserveroutes: Routes = [
    {
        path: '',
        component:ReservationComponent
        
    }
]